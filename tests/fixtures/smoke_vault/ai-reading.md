# AI Reading Notes

Hybrid retrieval combines semantic vector search with lexical keyword search.

This matters because exact names and keywords are often missed by semantic-only search, while broad conceptual matches are often missed by keyword-only search.

## Graph Memory

Graph memory should be added later for relationships, contradictions, timelines, and multi-hop questions.
